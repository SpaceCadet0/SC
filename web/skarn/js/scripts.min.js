
var navMain = document.querySelector(".menu"),
    navToggle = document.querySelector(".menu__toggle");
navMain.classList.remove("menu--nojs"), navToggle.addEventListener("click", function () {
        navMain.classList.contains("menu--closed") ? (navMain.classList.remove("menu--closed"), navMain.classList.add("menu--opened")) : (navMain.classList.add("menu--closed"), navMain.classList.remove("menu--opened"))
    }),
    function (o, e) {
        "use strict";
        var a = "img/symbols.svg",
            n = 1;
        if (!e.createElementNS || !e.createElementNS("http://www.w3.org/2000/svg", "svg").createSVGRect) return !0;
        var s, r, t = "localStorage" in o && null !== o.localStorage,
            i = function () {
                e.body.insertAdjacentHTML("afterbegin", r)
            },
            l = function () {
                e.body ? i() : e.addEventListener("DOMContentLoaded", i)
            };
        if (t && localStorage.getItem("inlineSVGrev") == n && (r = localStorage.getItem("inlineSVGdata"))) return l(), !0;
        try {
            s = new XMLHttpRequest, s.open("GET", a, !0), s.onload = function () {
                s.status >= 200 && s.status < 400 && (r = s.responseText, l(), t && (localStorage.setItem("inlineSVGdata", r), localStorage.setItem("inlineSVGrev", n)))
            }, s.send()
        } catch (p) {}
    }(window, document);

    // scroll
const anchors = document.querySelectorAll('a[href*="#"]')

for (let anchor of anchors) {
    anchor.addEventListener('click', function (e) {
        e.preventDefault()

        const blockID = anchor.getAttribute('href')

        document.querySelector('' + blockID).scrollIntoView({
            behavior: 'smooth',
            block: 'start'
        })
    })
}

    //tabs
    var tab; // заголовок вкладки
    var tabContent; // блок содержащий контент вкладки
    
    
    window.onload=function() {
        tabContent=document.getElementsByClassName('tab__panel');
        tab=document.getElementsByClassName('tab__title');
        hideTabsContent(1);
    }
    
    document.getElementById('tabs').onclick= function (event) {
        var target=event.target;
        if (target.className=='tab__title') {
           for (var i=0; i<tab.length; i++) {
               if (target == tab[i]) {
                   showTabsContent(i);
                   break;
               }
           }
        }
    }
    
    function hideTabsContent(a) {
        for (var i=a; i<tabContent.length; i++) {
            tabContent[i].classList.remove('show');
            tabContent[i].classList.add("hide");
            tab[i].classList.remove('tab__title--active');
        }
    }
    
    function showTabsContent(b){
        if (tabContent[b].classList.contains('hide')) {
            hideTabsContent(0);
            tab[b].classList.add('tab__title--active');
            tabContent[b].classList.remove('hide');
            tabContent[b].classList.add('show');
        }
    }

    
// var feedback = document.querySelector(".feedback__item"),
//       feedButton = document.querySelector(".feedback__button");







// var linkRoom = document.querySelector(".menu__user-login"),
//     popupRoom = document.querySelector(".popup-room"),
//     closeRoom = document.querySelector(".popup-room__close"),
//     overlay = document.querySelector(".overlay");
// linkRoom.addEventListener("click", function (o) {
//     o.preventDefault(), popupRoom.classList.add("popup-room--show"), overlay.classList.add("overlay--active"), login.focus()
// }), closeRoom.addEventListener("click", function (o) {
//     o.preventDefault(), popupRoom.classList.remove("popup-room--show"), popupRoom.classList.remove("popup-room--error"), overlay.classList.remove("overlay--active")
// }), overlay.addEventListener("click", function (o) {
//     o.preventDefault(), popupRoom.classList.remove("popup-room--show"), overlay.classList.remove("overlay--active"), popupSuccess.classList.remove("popup-success--show"), popupFailure.classList.remove("popup-failure--show")
// }), window.addEventListener("keydown", function (o) {
//     27 === o.keyCode && popupRoom.classList.contains("popup-room--show") && (popupRoom.classList.remove("popup-room--show"), overlay.classList.remove("overlay--active"))
// });
// var formRoom = popupRoom.querySelector(".popup-room__content"),
//     loginRoom = popupRoom.querySelector("[name=login]"),
//     passwordRoom = popupRoom.querySelector("[name=password]");
// formRoom.addEventListener("submit", function (o) {
//     loginRoom.value && passwordRoom.value || (o.preventDefault(), popupRoom.classList.add("popup-room--error"))
// });